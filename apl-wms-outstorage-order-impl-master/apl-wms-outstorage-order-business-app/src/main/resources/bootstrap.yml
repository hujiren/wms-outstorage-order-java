spring:
  application:
    name: apl-wms-outstorage-order-business-app

  profiles:
    include:
      common, apl-wms-outstorage-order-business-app
  cloud:
    consul:
      config:
        fail-fast: false
